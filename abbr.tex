% !TeX root = document.tex
% !TeX encoding = UTF-8 Unicode

% probably a good idea for the nomenclature entries:
\acsetup{list/template=longtable,make-links=true}

\ExplSyntaxOn
\cs_set_protected:Npn \acro_activate_hyperref_support:
  {
    \bool_if:nT { \l__acro_hyperref_loaded_bool }
      {
        \sys_if_engine_xetex:TF
          {
            \cs_set:Npn \acro_hyper_link:nn ##1##2
             { \hyperlink {##1} { \XeTeXLinkBox{##2} } } 
          }
          { \cs_set_eq:NN \acro_hyper_link:nn \hyperlink }
        \cs_set:Npn \acro_hyper_target:nn ##1##2
          { \raisebox { 3ex } [ 0pt ] { \hypertarget {##1} { } } ##2 }
      }
  }
\ExplSyntaxOff

\DeclareAcronym{CG}{
    short = \CG{},
    long  = Command Governor,
    tag   = abbrev
}

\DeclareAcronym{AT}{
    short = \(A^{\top}\),
    long  = Matrix transpose,
    sort  = A',
    tag   = nomencl
}

\DeclareAcronym{Reals}{
    short = \(\mathbb{R}\),
    long  = Set of real numbers,
    sort  = Reals,
    tag   = nomencl
}

\DeclareAcronym{RealVec}{
    short = \(\mathbb{R}^{n}\),
    long  = Vector of \(n\) real elements,
    sort  = Real Vector,
    tag   = nomencl
}

\DeclareAcronym{RealMatrix}{
    short = \(\mathbb{R}^{n\times{}m}\),
    long  = Matrix of \(n\) rows and \(m\) columns and real elements,
    sort  = Real Matrix,
    tag   = nomencl
}

\DeclareAcronym{Set}{
    short = \(\mathcal{C}\),
    long  = A set,
    sort  = set,
    tag   = nomencl
}

\DeclareAcronym{gk}{
    short = \(g(k)\),
    long  = Discrete time signal,
    sort  = dsignal,
    tag   = nomencl
}

\DeclareAcronym{gt}{
    short = \(g(t)\),
    long  = Continuous time signal,
    sort  = csignal,
    tag   = nomencl
}

\DeclareAcronym{WeighedNorm}{
    short = \(\norm{x}^2_\Psi{}\),
    long  = {\(x^\top\Psi{}x~|~x\in{}\mathbb{R}^n,~\Psi{} \in{} \mathbb{R}^{n \times n}\)},
    sort  = csignal,
    tag   = nomencl
}

\DeclareAcronym{gte}{
    short = \(\ge\),
    long  = Real number inequality,
    sort  = gte,
    tag   = nomencl
}

\DeclareAcronym{gtesdp}{
    short = \(\succ\),
    long  = Definite positive matrix inequality,
    sort  = gtesdp,
    tag   = nomencl
}
 
\acuseall{}
